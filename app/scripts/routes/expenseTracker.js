/*global define*/

define([
  'jquery',
  'backbone'
], function ($, Backbone) {
  'use strict';

  var ExpenseTrackerRouter = Backbone.Router.extend({
    routes: {
    }

  });

  return ExpenseTrackerRouter;
});
